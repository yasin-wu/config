qodana:
  stage: qodana
  image:
    name: jetbrains/qodana-go:2022.3-eap
    entrypoint: [""]
  only:
    - develop
  tags:
    - ${RUNNER}
  script:
    - qodana scan --save-report --results-dir=$CI_PROJECT_DIR/qodana
    #### show report
    #cd $CI_PROJECT_DIR/qodana/report
    #python2 -m SimpleHTTPServer
    #python3 -m http.server
    #docker run -it --rm -p 8000:80 -v $(pwd):/usr/share/nginx/html nginx
  artifacts:
    paths:
      - qodana