review_code:
  stage: review_code
  allow_failure: true
  only:
    - develop
  tags:
    - ${RUNNER}
  script:
    - echo `golangci-lint run --timeout=10m --out-format=tab > golangci-lint.txt`
    - cat golangci-lint.txt
  artifacts:
    expire_in: 1d
    paths:
      - golangci-lint.txt

