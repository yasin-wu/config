review_code:
  stage: review_code
  tags:
    - ${RUNNER}
  script:
    - go mod tidy
    - golangci-lint run --timeout=10m --output.junit-xml.path=golangci-lint.xml
  artifacts:
    reports:
      junit: golangci-lint.xml