deploy:
  stage: deploy
  rules:
    - if: '$CI_COMMIT_BRANCH =~ /^feature-.*/'
    - if: '$CI_COMMIT_BRANCH == "develop"'
  tags:
    - ${RUNNER}
  script:
    - kubectl rollout restart deploy -n yasin ${APP}